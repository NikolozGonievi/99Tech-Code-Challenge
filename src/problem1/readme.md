Solution Notes

Assumptions: 
    For this task, I assume that the input n is a positive integer.

Approaches: 
    sum_to_n_a — iterative approach (O(n))
    sum_to_n_b — mathematical formula (O(1))
    sum_to_n_c — recursive approach (O(n), uses call stack)

Run: 
    node index.js